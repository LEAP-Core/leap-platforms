- Makefile
  Makefile for driver. Before building, update the environment as follows:
      unsetenv ARCH
      setenv PATH /usr/bin:$PATH

- pchnl_if.h
  PCIe Hardware Channel interface declaration

- pchnl_if.awb
  AWB Module Wrapper around pchnl_if.h

- pchnl_main.[c|h]
  PCIe Hardware Channel driver
